# BuildCommand AI — Render production environment

Set these on the BuildCommand web service:

DATABASE_URL=<Render Postgres INTERNAL database URL>
UPLOAD_DIR=/var/data/buildcommand_uploads
OPENAI_API_KEY=<existing key>
COOKIE_SECURE=1

# Persistent Disk
Mount path: /var/data

# Optional email alerts
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM=
SMTP_USER=
SMTP_PASSWORD=

# Recommended health check
/health
